@Library('shared-library') _

pipeline{
    agent any
    stages{
        stage('variable test'){
            steps{
                helloworld(name: 'Karan', day:'Monday')
            }
        }
    }
}
